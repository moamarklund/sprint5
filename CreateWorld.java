public class CreateWorld{ 
   
    
    //Attribut 

    /*	while(true){ 
	    Random random = new Random(); 
	    int randomNumber =  random.nextInt(4);//the 4 is exclusive
	    if(randomNumber > 0) {
		int weight = randomNumber;
		break; 
	    } 
	    } */

}
